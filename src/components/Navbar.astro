---
import MobileNav from "./MobileNav";

export const menuItems = [
  { label: "home", url: "/" },
  { label: "about", url: "#about" },
  { label: "contact", url: "#contact" },
  { label: "blog", url: "/blog" },
];
---

<nav
  class="bg-darkColorPrimary text-lightColor text-3xl border-b-2 border-lightColor"
>
  <div
    class="max-w-5xl container mx-auto px-8 py-5 flex justify-between items-center gap-10"
  >
    <h1
      class="font-bold text-4xl text-gradient hover:scale-105 transition-all duration-700"
    >
      <a href="/">mortdotdev</a>
    </h1>
    <ul class="lg:flex gap-10 hidden">
      {
        menuItems.map(({ label, url }) => (
          <li class=" hover:text-yellowColor transition-all duration-500 hover:scale-105 font-semibold">
            <a href={url}>{label}</a>
          </li>
        ))
      }
    </ul>
    <div class="lg:hidden">
      <MobileNav client:load />
    </div>
  </div>
</nav>

<style is:global>
  nav h1 a {
    font-family: "Protest Guerrilla", sans-serif;
  }
</style>
